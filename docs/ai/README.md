# AI Memory

Load order:

1. `STATUS.md`
2. `CONTEXT.md` when architecture context is needed
3. `RUNBOOK.md` when running commands
4. `UPSTREAM.md` when syncing with upstream
5. `daily/*.md` for recent session history

This project is an upstream-compatible QMD secondary development. Keep memory concise and update `STATUS.md` plus the latest `daily/` log at the end of each work session.
